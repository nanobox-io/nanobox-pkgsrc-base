$NetBSD$

--- src/third_party/wiredtiger/SConscript.orig	2017-05-01 20:06:54.000000000 +0000
+++ src/third_party/wiredtiger/SConscript
@@ -59,7 +59,7 @@ if (VERSION_MAJOR == None or
     VERSION_MINOR == None or
     VERSION_PATCH == None or
     VERSION_STRING == None):
-    print "Failed to find version variables in " + version_file
+    print("Failed to find version variables in " + version_file)
     Exit(1)
 
 wiredtiger_includes = """
